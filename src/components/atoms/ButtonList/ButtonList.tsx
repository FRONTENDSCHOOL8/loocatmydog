import styled from 'styled-components';

const StyledButtonList = styled.div`
  inline-size: 90px;
  block-size: 60px;
  display: flex;
  flex-direction: column;
`;
const ButtonList = () => {
  return <StyledButtonList></StyledButtonList>;
};

export default ButtonList;
